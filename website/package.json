{
  "name": "website",
    "scripts": {
        "build": "echo $BASE_URL; hugo --minify --baseURL=${BASE_URL}"
    },
    "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.43",
    "postcss-cli": "^11.0.0"
  }
}
