{
  "name": "website",
    "scripts": {
        "build": "echo $BASE_URL; hugo --minify --baseURL=${BASE_URL}"
    },
    "devDependencies": {
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.8",
    "postcss-cli": "^11.0.1"
  }
}
